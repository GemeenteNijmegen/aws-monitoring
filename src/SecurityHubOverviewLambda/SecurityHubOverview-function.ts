// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for SecurityHubOverviewFunction
 */
export interface SecurityHubOverviewFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/SecurityHubOverviewLambda/SecurityHubOverview.
 */
export class SecurityHubOverviewFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: SecurityHubOverviewFunctionProps) {
    super(scope, id, {
      description: 'src/SecurityHubOverviewLambda/SecurityHubOverview.lambda.ts',
      ...props,
      runtime: lambda.Runtime.NODEJS_14_X,
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../assets/SecurityHubOverviewLambda/SecurityHubOverview.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}