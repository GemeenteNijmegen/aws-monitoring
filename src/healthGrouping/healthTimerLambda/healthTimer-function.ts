// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for HealthTimerFunction
 */
export interface HealthTimerFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/healthGrouping/healthTimerLambda/healthTimer.
 */
export class HealthTimerFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: HealthTimerFunctionProps) {
    super(scope, id, {
      description: 'src/healthGrouping/healthTimerLambda/healthTimer.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs24.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/healthGrouping/healthTimerLambda/healthTimer.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}