// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for SlackbotFunction
 */
export interface SlackbotFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/audit/slackbot/slackbot.
 */
export class SlackbotFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: SlackbotFunctionProps) {
    super(scope, id, {
      description: 'src/audit/slackbot/slackbot.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs24.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/audit/slackbot/slackbot.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}